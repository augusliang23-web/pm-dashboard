{
  "name": "pm-dashboard-production-security-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test:all": "node --test $(find tests -maxdepth 1 -name '*.test.mjs' ! -name 'firestore-rules-emulator.test.mjs' -print) functions/test/*.test.cjs",
    "test:rules": "FIRESTORE_EMULATOR_PORT=8081 firebase emulators:exec --config firebase.rules-test.json --only firestore --project demo-pm-dashboard-v22t \"node --test tests/firestore-rules-emulator.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase": "^12.17.1",
    "firebase-tools": "^15.24.0"
  }
}
