{
  "name": "pm-dashboard-production-security-tests",
  "private": true,
  "type": "module",
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase": "^12.17.1",
    "firebase-tools": "^15.24.0"
  },
  "scripts": {
    "test:all": "node --test *.test.cjs *.test.mjs $(find tests -maxdepth 1 -name '*.test.mjs' ! -name 'firestore-rules-emulator.test.mjs' -print) functions/test/*.test.cjs",
    "test:rules:prod": "FIRESTORE_EMULATOR_PORT=8081 firebase emulators:exec --config firebase.rules-test.json --only firestore --project demo-pm-dashboard-v22t \"node --test tests/firestore-rules-emulator.test.mjs\"",
    "test:rules:uat": "FIRESTORE_EMULATOR_PORT=8081 FIRESTORE_RULES_FILE=firestore.uat.rules firebase emulators:exec --config firebase.uat.rules-test.json --only firestore --project demo-pm-dashboard-v22t \"node --test tests/firestore-rules-emulator.uat.test.mjs\"",
    "test:rules:shared-backend": "FIRESTORE_EMULATOR_PORT=8082 FIRESTORE_RULES_FILE=firestore.shared-backend.rules firebase emulators:exec --config firebase.shared-backend.rules-test.json --only firestore --project demo-pm-dashboard-v22t \"node --test tests/firestore-rules-emulator.uat.test.mjs\"",
    "test:rules": "npm run test:rules:prod && npm run test:rules:uat && npm run test:rules:shared-backend",
    "local:start": "node scripts/local-stack.mjs",
    "local:stop": "node scripts/local-stop.mjs",
    "local:seed": "node scripts/local-stack.mjs --seed-only",
    "verify:sync-boundary": "node scripts/verify-production-sync-boundary.mjs",
    "test:local": "node scripts/local-health.mjs",
    "verify:local": "npm run test:all && npm run test:local && git diff --check",
    "deploy": "node scripts/deploy-after-verify.mjs"
  }
}
